# ------------------------------------------------------
#        Data sticher configuration
# ------------------------------------------------------
# This is an example used for single time series input
data_labels:
  fpr: "fpr"
  tpr: "tpr"

  # STEP3/STEP4 is the model training and evaluation steps
  ad:
    ground_truth: "label"
    prediction: "result"

  # STEP5 this step is used only in the statistical ML based
  #  anomaly detection models
  cd:
    ground_truth: "cd_label"
    prediction: "cd_result"
